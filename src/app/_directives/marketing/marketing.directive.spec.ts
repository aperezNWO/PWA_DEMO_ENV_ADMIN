import { MarketingSortableHeader } from './marketing.directive';

describe('MarketingDirective', () => {
  it('should create an instance', () => {
    const directive = new MarketingSortableHeader();
    expect(directive).toBeTruthy();
  });
});
