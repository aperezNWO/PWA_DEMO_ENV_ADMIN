import { NodeJsDirective } from './nodeJsDemo/node-js.directive';

describe('NodeJsDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeJsDirective();
    expect(directive).toBeTruthy();
  });
});
