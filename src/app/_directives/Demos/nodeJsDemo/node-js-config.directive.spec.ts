import { NodeJsConfigListSortableHeader } from './node-js-config.directive';

describe('NodeJsConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new NodeJsConfigListSortableHeader();
    expect(directive).toBeTruthy();
  });
});
