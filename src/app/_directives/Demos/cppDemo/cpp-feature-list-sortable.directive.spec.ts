import { CppFeatureListSortableHeader } from './cpp-feature-list-sortable.directive';

describe('CppFeatureListSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new CppFeatureListSortableHeader();
    expect(directive).toBeTruthy();
  });
});
